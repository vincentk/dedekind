name: C++ CI with Cmake

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PYTHON_VERSION: "3.12"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write  # required by codecov-action to post PR coverage comments
    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      with:
        submodules: recursive

    - name: Set up Python
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install clang build chain
      run: |
        wget https://apt.llvm.org/llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh 22
        sudo apt-get install -y clang-22 ninja-build clang-format-22 llvm-22

    - name: Check Code Formatting
      run: make format-check CLANG_FORMAT=clang-format-22

    - name: Build & Run Integration Tests
      run: make integration-test CXX=clang++-22 CC=clang-22 CMAKE_EXTRA_ARGS="-DDEDEKIND_RUNTIME_COVERAGE_BRIDGES=ON"

    - name: Validate Checked-In LLVM IR Fixture
      run: make ir-fixture-check CXX=clang++-22

    - name: Generate & Upload LLVM IR Fixtures
      if: ${{ always() }}
      run: make ir-fixture-refresh CXX=clang++-22
      continue-on-error: true
    - name: Upload LLVM IR Fixtures
      if: ${{ always() }}
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
      with:
        name: llvm-ir-fixtures
        if-no-files-found: warn
        path: src/test/cpp/modules/dedekind/python/*.ll

    - name: Upload Notebook Integration Artifacts
      if: ${{ always() }}
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
      with:
        name: python-notebooks
        if-no-files-found: warn
        path: |
          build/python-notebooks/*.ipynb
          build/python-notebooks/integration-summary.txt

    - name: Process Coverage Data
      run: make coverage CXX=clang++-22 CC=clang-22 CMAKE_EXTRA_ARGS="-DDEDEKIND_RUNTIME_COVERAGE_BRIDGES=ON"

    - name: Collect Python Coverage
      run: |
        make python-coverage
        grep -q "python/dedekind/" build/python-coverage.xml

    - name: Upload to Codecov
      uses: codecov/codecov-action@75cd11691c0faa626561e295848008c8a7dddffe  # v5
      with:
        files: build/coverage.info,build/python-coverage.xml
        token: ${{ secrets.CODECOV_TOKEN }}
        fail_ci_if_error: true
        verbose: true

  python-dist:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build-and-deploy
    runs-on: ubuntu-latest
    permissions:
      contents: read  # checkout only; no PR write or pages needed
    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      with:
        submodules: recursive

    - name: Set up Python
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install clang build chain
      run: |
        wget https://apt.llvm.org/llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh 22
        sudo apt-get install -y clang-22 ninja-build clang-format-22

    - name: Build Python Distribution Artifacts
      env:
        CC: clang-22
        CXX: clang++-22
        CMAKE_ARGS: >-
          -DCMAKE_C_COMPILER=clang-22
          -DCMAKE_CXX_COMPILER=clang++-22
          -DCMAKE_CXX_SCAN_FOR_MODULES=ON
      run: |
        python -m pip install --upgrade pip build twine
        python -m build
        python -m twine check dist/*
        mkdir -p build/python-docs
        ls -lh dist | tee build/python-docs/dist-manifest.txt
        sha256sum dist/* | tee build/python-docs/dist-sha256.txt

    - name: Verify Installed Wheel Smoke Test
      run: |
        python -m venv .venv-wheel-smoke
        . .venv-wheel-smoke/bin/activate
        python -m pip install --upgrade pip
        python -m pip install dist/*.whl
        mkdir -p build/python-docs
        python -m pip show dedekind > build/python-docs/install-report.txt
        python -m pydoc dedekind > build/python-docs/pydoc-dedekind.txt
        python - <<'PY'
        import dedekind

        assert dedekind.ordered_set_roundtrip([3, 1, 2, 2]) == [1, 2, 3]
        assert dedekind.unordered_set_roundtrip([4, 2, 4, 1]) == [1, 2, 4]
        assert dedekind.path_from_range([2, 4, 6, 8]) == [2, 4, 6, 8]
        print("python-wheel-smoke-ok")
        PY

    - name: Upload Python Dist Artifacts
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
      with:
        name: python-dist
        path: |
          dist/*
          build/python-docs/dist-manifest.txt
          build/python-docs/dist-sha256.txt
          build/python-docs/install-report.txt
          build/python-docs/pydoc-dedekind.txt

  docs-and-pages:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build-and-deploy
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write       # required by actions/upload-pages-artifact + deploy-pages
      id-token: write    # required by actions/deploy-pages (OIDC verification)
    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      with:
        submodules: recursive

    - name: Set up Python
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install clang build chain
      run: |
        wget https://apt.llvm.org/llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh 22
        sudo apt-get install -y clang-22 ninja-build clang-format-22

    - name: Install docs toolchains
      run: |
        make ci-install-doxygen-deps
        make ci-install-report-deps

    - name: Build Doxygen and report in parallel
      run: |
        (
          make doxygen CXX=clang++-22 CC=clang-22 CMAKE_EXTRA_ARGS="-DDEDEKIND_RUNTIME_COVERAGE_BRIDGES=ON"
        ) &
        DOXYGEN_BUILD_PID=$!

        (
          make report
          make -C docs/report doc
        ) &
        REPORT_BUILD_PID=$!

        wait "$DOXYGEN_BUILD_PID"
        wait "$REPORT_BUILD_PID"

    - name: Stage Pages content
      run: |
        mkdir -p build/pages
        cp -R build/docs/html/. build/pages/
        cp docs/report/report.pdf build/pages/report.pdf

    - name: Upload artifact
      uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b  # v4
      with:
        path: 'build/pages'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e  # v4
