/**
 * @file dedekind.cppm
 * @brief This module provides core math operations for the library.
 */

export module dedekind;

export namespace dedekind {
int add(int a, int b) { return a + b; }
}  // namespace dedekind
