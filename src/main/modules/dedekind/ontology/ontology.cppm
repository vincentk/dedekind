export module dedekind.ontology;

// --- Partitions ---
export import :mereology; // Sets, Lattices, Membership
export import :topology;  // Convexity, Rays, Intervals
export import :algebra;   // Monoids, Groups, Fields
export import :density;   // Continuity, Archimedean property

/**
 * @brief The Unified Dedekind Ontology.
 * Ad Fontes: This module acts as the "Registry of Truths" 
 * for the entire structural system.
 */
