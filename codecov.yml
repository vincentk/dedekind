# Map the .cppm extension to the C++ language
# This ensures Codecov actually "looks" at the hits for your partitions
# as described in Section 4.4 of the draft.
coverage:
  status:
    patch:
      default:
        target: 60%  # Matches your grounded goal in the paper (p. 13)

